server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exam_helper?useSSL=false&serverTimezone=UTC
    username: root
    password: 27dlfwjsdur!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update     # 테이블 자동 생성
    show-sql: true         # SQL 로그 콘솔에 출력
    database-platform: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
